[package]
name = "content_types"
version = "0.0.1"
workspace = "../../"

[dependencies]
rocket = { path = "../../lib" }
rocket_codegen = { path = "../../codegen" }
serde = "0.9"
serde_json = "0.9"
serde_derive = "0.9"

[dev-dependencies]
rocket = { path = "../../lib", features = ["testing"] }
