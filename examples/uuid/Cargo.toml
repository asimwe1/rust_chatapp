[package]
name = "uuid"
version = "0.1.0"
workspace = "../../"

[dependencies]
rocket = { path = "../../lib" }
rocket_codegen = { path = "../../codegen" }
uuid = ">= 0.5, <= 0.6"
lazy_static = "1.0"

[dependencies.rocket_contrib]
default-features = false
path = "../../contrib"
features = ["uuid"]
