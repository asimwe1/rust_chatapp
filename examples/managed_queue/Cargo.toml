[package]
name = "managed_queue"
version = "0.0.0"
workspace = "../.."
publish = false

[dependencies]
crossbeam = "*"
rocket = { path = "../../core/lib" }
rocket_codegen = { path = "../../core/codegen" }
